# wiki

Personal notes and Python experiments published with Quarto.

```bash
uv sync
uv run quarto preview
```

Experiments are percent-format `.py` scripts. Run `uv run quarto render` to
refresh changed computations and their committed `_freeze/` output.
